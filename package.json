{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./server/bin/www",
    "heroku-postbuild": "cd back-end && npm install && cd ../voniel-brown-portfolio && npm install && npm run build"
  }
}
